sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins


---------------
sudo apt update
sudo apt install fontconfig openjdk-21-jre
java -version


-----

sudo service jenkins stop
sudo apt-get remove --purge jenkins

---
# for ARM systems, set ARCH to: `arm64`, `armv6` or `armv7`
ARCH=amd64
PLATFORM=$(uname -s)_$ARCH

curl -sLO "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_$PLATFORM.tar.gz"

# (Optional) Verify checksum
curl -sL "https://github.com/eksctl-io/eksctl/releases/latest/download/eksctl_checksums.txt" | grep $PLATFORM | sha256sum --check

tar -xzf eksctl_$PLATFORM.tar.gz -C /tmp && rm eksctl_$PLATFORM.tar.gz

sudo install -m 0755 /tmp/eksctl /usr/local/bin && rm /tmp/eksctl
---

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew tap aws/tap
brew install aws/tap/eksctl


-------------
docker run --rm -it public.ecr.aws/eksctl/eksctl version


-----------------
persistentpoornendrabpersistentcompoornendrabpersistentcom


pass -a63c-5-41L14+
https://463470972400.signin.aws.amazon.com/console



--------------------------------------latest---------Chat-------------------
🐳 Docker Installation (Official & Stable – Ubuntu)

👉 This follows Docker’s official docs (simplified but correct)

🔹 Remove old versions (safe)
sudo apt-get remove docker docker-engine docker.io containerd runc -y
🔹 Install dependencies
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg
🔹 Add Docker GPG key
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
🔹 Add Docker repository
echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
🔹 Install Docker
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
🔹 Start Docker
sudo systemctl start docker
sudo systemctl enable docker
🔹 Verify
docker --version
🔹 Give access (IMPORTANT)
sudo usermod -aG docker ubuntu
newgrp docker


⚙️ Jenkins Installation (Official – FIXED version)

👉 Your lab used a wrong/old key URL, causing GPG error
👉 Use this corrected official method

🔹 Install Java (required)
sudo apt-get install -y fontconfig openjdk-17-jre
🔹 Add Jenkins key (CORRECT)
sudo wget -O /usr/share/keyrings/jenkins-keyring.asc \
https://pkg.jenkins.io/debian-stable/jenkins.io.key
🔹 Add repository
echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
https://pkg.jenkins.io/debian-stable binary/" | \
sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
🔹 Install Jenkins
sudo apt-get update
sudo apt-get install jenkins -y
🔹 Start Jenkins
sudo systemctl start jenkins
sudo systemctl enable jenkins
🔹 Check status
sudo systemctl status jenkins
🔹 Get admin password
sudo cat /var/lib/jenkins/secrets/initialAdminPassword
🔹 Access UI
http://<EC2-IP>:8080



------------------------------claud-----------------
1. Prerequisites
Update your system before starting:
bashsudo apt-get update && sudo apt-get upgrade -y
Ensure the following packages are available:
bashsudo apt-get install -y curl wget gnupg2 software-properties-common \
  apt-transport-https ca-certificates lsb-release
Remove Conflicting Docker Packages (if any)
Before installing Docker, remove any unofficial versions that may already be installed:
bashfor pkg in docker.io docker-doc docker-compose docker-compose-v2 \
  podman-docker containerd runc; do
  sudo apt-get remove $pkg -y
done

2. Install Java
Jenkins requires Java 17 or later. Install OpenJDK 17:
bashsudo apt-get update
sudo apt-get install -y fontconfig openjdk-17-jre
Verify the installation:
bashjava -version
Expected output:
openjdk version "17.x.x" ...
OpenJDK Runtime Environment ...
OpenJDK 64-Bit Server VM ...

⚠️ Note: Do NOT use default-jdk — it may install an older Java version incompatible with the latest Jenkins LTS releases.


3. Install Docker
Step 1 — Add Docker's Official GPG Key
bashsudo install -m 0755 -d /etc/apt/keyrings

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

sudo chmod a+r /etc/apt/keyrings/docker.gpg
Step 2 — Add Docker Repository
bashecho \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
Step 3 — Install Docker Engine
bashsudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io
Step 4 — Start & Enable Docker
bashsudo systemctl start docker
sudo systemctl enable docker
Step 5 — Verify Docker Installation
bashsudo systemctl status docker
Expected output:
● docker.service - Docker Application Container Engine
   Active: active (running) ...
Run a test container to confirm Docker works:
bashsudo docker run hello-world

4. Install Jenkins
Step 1 — Add Jenkins GPG Key
bashsudo wget -O /etc/apt/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
Step 2 — Add Jenkins Repository
bashecho "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian-stable binary/" | \
  sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
Step 3 — Install Jenkins
bashsudo apt-get update
sudo apt-get install -y jenkins
Step 4 — Start & Enable Jenkins
bashsudo systemctl start jenkins
sudo systemctl enable jenkins
Step 5 — Verify Jenkins is Running
bashsudo systemctl status jenkins
Expected output:
● jenkins.service - Jenkins Continuous Integration Server
   Active: active (running) ...

5. Start & Verify Services
Use the following commands to check the status of both services at any time:
ServiceStartEnable on BootStatusDockersudo systemctl start dockersudo systemctl enable dockersudo systemctl status dockerJenkinssudo systemctl start jenkinssudo systemctl enable jenkinssudo systemctl status jenkins

6. Unlock Jenkins via Browser
Step 1 — Open Jenkins in Browser
http://<your-EC2-public-IP>:8080

Make sure port 8080 is open in your EC2 security group inbound rules.

Step 2 — Get the Initial Admin Password
bashsudo cat /var/lib/jenkins/secrets/initialAdminPassword
Copy the output (a 32-character alphanumeric string).
Step 3 — Complete Setup Wizard

Paste the password into the Administrator password field → click Continue
Select Install suggested plugins and wait for installation to finish
Create the first admin user:

FieldValueUsernamejenkins-admin-userPasswordjenkins_springboot4684$Full Name(your choice)Email(your choice)

Click Save and Continue → Save and Finish → Start using Jenkins


7. Post-Installation: Add Jenkins to Docker Group
This allows Jenkins to run Docker commands without sudo:
bash# Add Jenkins user to the Docker group
sudo usermod -a -G docker jenkins

# Restart services to apply changes
sudo service jenkins restart
sudo systemctl daemon-reload
sudo service docker stop
sudo service docker start
Verify by switching to the Jenkins user and checking Docker access:
bashsudo su -s /bin/bash jenkins
whoami       # should output: jenkins
docker ps    # should run without permission errors
